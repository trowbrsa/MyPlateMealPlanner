<h1>Welcome to MyPlate Meal Planner</h1>

<table>
  <thead>
    <tr>
      <td></td>
      <% Meal::DAYS.each do |day| %>
      <th>
        <%= day %>
      </th>
      <% end %>
    </tr>
  </thead>
  <tbody>
      <% Meal::MEALS.each do |meal| %>
      <tr>
      <th>
        <%= meal %>
      </th>
        <% Meal::DAYS.each do |day| %>
        <% day_meal = Meal.where("meal = ? AND day = ?", meal, day ) %>
        <td>
          <% if day_meal == [] %>
            <%= link_to "Add Meal", "/meals/new/?day=#{day}&meal=#{meal}" %>
          <% else %>
            <% day_meal.each do |m| %>
              <%= link_to m.meal_name , meal_path(m) %>
            <% end %>
          <% end %>
        </td>
        <% end %>
      </tr>
      <% end %>
  </tbody>
</table>
